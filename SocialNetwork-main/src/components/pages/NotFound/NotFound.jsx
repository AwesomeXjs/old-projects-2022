import React from 'react'
import Loader from '../../../custom/UI/NotFoundPage/loader'
import styles from './NotFound.module.scss'

export const NotFound = () => {
	return <Loader fontSize={'3rem'}>ERROR 404. NOT FOUND PAGE</Loader>
}
